import { Text, View } from 'react-native';

export default function AudioSelectionScreen() {
    return (
        <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
            <Text>Audio Selection Screen</Text>
        </View>
    );
}
